var a = 10, b = 23;
a = ++b
console.log(a, b);

/* when the increment operator is placed before the operand,
   it returns the incremented value, and when it is placed
   after the operand, it returns the original value and 
   increases the operand.
*/