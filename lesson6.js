// Functions

function greet(name, lastName) {   //name and lastName here are called parameters.
    console.log( 'Hello ' + name + ' ' + lastName);
}

greet('Gideon', 'Eso') //Gideon here, is called an argument which is the actual value of the parameter declared with the function.
greet('Stacy', 'Idundun')



//Function declarations do not require semi-colons at the end.