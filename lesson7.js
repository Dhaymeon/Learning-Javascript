// Performing a task

function greet(name, lastName) {  
    console.log( 'Hello ' + name + ' ' + lastName);
}

// Calculating a value
function square(number) {
   return number * number;
}

console.log(square(2));

// A function is a set of statements that either performs a task or calculates and reurns a value.
