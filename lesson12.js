let a = 65;
let b = 58;
if (a>b) {
    alert('this is a nice way to get the hang of coding')
}

function greet(name) {
    console.log('Hello ' + name)
}

greet('Gideon')

//The "if" statement is a conditional statement that is used to
// execute a line(or lines) of code if the condition is true.
//If the condition is false, it will be skipped and the next line of code would be executed.