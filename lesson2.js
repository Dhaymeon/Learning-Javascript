const interestRate = 0.3;
interestRate = 1
console.log(interestRate);

// For variables the 'let' reserved keyword is used, hence, the value can be contorted at any point in the code.
// While for constants, the 'const' reserved keyword is used and would prompt an error message (Uncaught TypeError) if the value is contorted from its original.
